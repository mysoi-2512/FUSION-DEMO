Utilities
==============
The utilities module consists of helper components, callbacks, wrappers, and setup scripts — each playing a supporting role
to simplify or streamline key processes during training and evaluation.

When building environments, caching embeddings, tracking fragmentation, or configuring learning rates,
these files offload the logic so that core files stays clean and modular.

.. toctree::

    cache_gnn_once
    callbacks
    deep_rl
    general_utils
    gym_envs
    hyperparams
    observation_space
    rl_zoo
    setup
    sim_data
    sim_env
    sim_filters
    topology
    unity_hyperparams


