Base Agent
==============

The ``base_agent.py`` module provides the ``BaseAgent`` class, a shared superclass for RL agents handling
path, core, and spectrum decisions. It encapsulates algorithm setup, reward strategies, and model loading.

.. automodule:: reinforcement_learning.agents.base_agent
    :members:
    :undoc-members:
    :private-members: